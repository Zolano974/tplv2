{% extends "FirstBundle:Layouts:layout.html.twig" %}

{% block title %}
    Penses Bêtes

{% endblock  %}

{% block content %}
    <div class="container">


        <div class="row">
            {% for x in xcoords %}
                {% for y in ycoords %}
                    {% set reminder = matrix[x][y] %}
                    <div class="col-md-4 reminder">
                        <p>
                            {{ reminder.xCoord }} : {{ reminder.yCoord }}
                        </p>
                    </div>
                {% endfor %}
            {% endfor %}
        </div>

    </div>


{% endblock  %}